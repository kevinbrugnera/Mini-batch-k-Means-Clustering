# Specify your master's node IP
MASTER_IP="10.67.22.135"

# If you are in a local enviroment make sure to keep this variable an empty string. Otherwise specify your workers node IPs without spaces after commas
WORKER_IPS=""

#Change the name of your workers user (we used this since we ran everything on CloudVeneto)
WORKER_USER="ubuntu"

# Fast copy to switch between local enviroment and our cluster
# WORKER_IPS="10.67.22.129,10.67.22.84,10.67.22.23,10.67.22.226"
